//declaration
function makeBread(qty) {
  const bread = "🍞".repeat(qty);
  //return value
  return bread;
}
const loaves = makeBread(7);
//task
console.log(loaves);

//function expression
const makeBeer
